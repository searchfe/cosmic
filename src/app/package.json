{
    "name": "@cosmic/app",
    "version": "0.0.1",
    "type": "commonjs",
    "main": "dist/index.js",
    "scripts": {
        "build": "pnpm i --shamefully-hoist && tsup && electron-builder",
        "dev": "tsup --watch",
        "start": "electron dist",
        "clean": "rimraf dist",
        "nodemon": "nodemon"
    },
    "dependencies": {
        "@cosmic/site": "workspace:*",
        "electron-log": "^4.4.1",
        "electron-store": "^8.0.1"
    },
    "devDependencies": {
        "electron": "^16.0.1",
        "electron-builder": "^22.13.1",
        "nodemon": "^2.0.15"
    }
}
